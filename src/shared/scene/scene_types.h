#pragma once

namespace pbr::shared::scene {
    /// The types of scenes implemented
    enum class scene_types {
        /// The loading scene
        loading,

        /// The splash screen
        splash_screen,

        /// The world generation scene
        world_generation,
    };
}
