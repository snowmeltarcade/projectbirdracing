#pragma once

namespace pbr::shared::apis::graphics {
    /// The types of supported shaders
    enum class shader_types {
        /// A vertex shader
        vertex,

        /// A fragment shader
        fragment,
    };
}
