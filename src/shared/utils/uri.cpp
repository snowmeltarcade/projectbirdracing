#include "uri.h"

namespace pbr::shared::utils {
    std::optional<uri> build_uri(const std::string& s) noexcept {
        if (s.empty()) {
            return {};
        }
        return {};
    }
}
